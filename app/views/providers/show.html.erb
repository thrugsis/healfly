<!DOCTYPE html>
<html>
<head>

</head>
<body>
<h1>Profile</h1>
  <p>
    <%= image_tag current_user.image + "?type=large" %>
  </p>

<div>Username:  <%= current_user.username %>
</div>
</div><br>
<div>Language:  <%= @provider.language%>
	<%#= form_tag provider_path,method: :get, remote: true do %>
	<%#= submit_tag 'Edit' %>
	<%# end %>
</div><br>
<div>Gender:  <%= current_user.gender %>
	<%= link_to 'Edit', edit_provider_path(@provider) %>
</div><br>
<div>Contact: <%= current_user.phone_number %>
	<%= link_to 'Edit', edit_provider_path(@provider) %>
</div><br>


<div id="appointmentRecord">
	
</div>
</body>
</html>

<% if signed_in? == false %>
  <% controller.redirect_to welcome_index_path %>
<% else %>
  <%= link_to 'Edit', edit_provider_path(@provider) %> |
  <%= link_to 'Back', providers_path %>
<% end %>

Just displaying the image and the qualification
<img src=<%= @provider.image.first.url %> ><br/>
<% if @provider.qualification.first.url.include?".pdf" %>
<embed src="https://drive.google.com/viewerng/viewer?embedded=true&url=<%= @provider.qualification.first.url %>" width="500" height="375">
<% else %>
<img src=<%= @provider.qualification.first.url %> > <br/>
<%end%>


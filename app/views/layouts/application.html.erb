<!DOCTYPE html>

<html>
   	<link rel="stylesheet" href="https://npmcdn.com/flatpickr/dist/themes/material_green.css">
   	<script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
  <head>
    
    <title>HEALFLY</title>
    <%= csrf_meta_tags %>
    
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">


    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  
  </head>

  <body>
    
    <!-- Clearance sign in button: -->
    <% if signed_in? %>
      Signed in as: <%= current_user.email %>
      <%= button_to 'Sign out', sign_out_path, method: :delete, class: "button" %>
    <% else %>
      <%= link_to 'Sign In', sign_in_path %>
    <% end %>

    <!-- Cae's Styling -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a id="navbar_logo" href="#">HealFly</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav mr-auto mt-2 mt-lg-0 navbar_futura_font">
                </li> 
                 <li class="nav-item dropdown">
                  <a id="beige_text" class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    join
                  </a>
                  <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="#">new patient</a>
                    <a class="dropdown-item" href="#">new provider</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="/auth/facebook"> sign up via facebook</a>
                  </div> 
                </li>
                <li class="nav-item">
                  <a class="nav-link disabled" href="#"> / </a>
                <li class="nav-item dropdown">
                  <a id="beige_text" class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    login
                  </a>
                  <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="#">patient</a>
                    <a class="dropdown-item" href="#">provider</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="/auth/facebook"> login via facebook</a>
                  </div>
              </ul>
            </div>
          </nav>
<!--       <%= link_to 'Sign In As Patient', user_sign_in_path %>
      <%= link_to 'Sign In As Provider', provider_sign_in_path %>  -->


    <!-- Clearance flash notice -->
     <div id="flash">
      <% flash.each do |key, value| -%>
        <div id="flash_<%= key %>"><%=h value %></div>
      <% end %>
    </div>
    <br>

    <%= yield %>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
  </body>
</html>
